@model IEnumerable<BnGClub.Models.Leader>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.leaderFName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.leaderMName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.leaderLName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.leaderEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.leaderPhone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.leaderFName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.leaderMName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.leaderLName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.leaderEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.leaderPhone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
